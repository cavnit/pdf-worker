[project]
name = "pdf-worker"
version = "0.1.0"
description = "PDF rasterisation and text extraction microservice"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.135.2",
    "pymupdf>=1.25.0",
    "python-multipart>=0.0.22",
]
